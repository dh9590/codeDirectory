

<modify and save the file>
git status -- file is red
git add .
git status -- file is green
git commit -am "third commit"
git push origin master
<refresh github in browser>

Attempted to add to VisualStudio Code on Dell Latitude
Attempt success.  Another modification of the file.
attempt Success.  another modification of this file.

FROM THE DELL LATITUDE VS Code
    *** (be in ...\Documents\codeDirectory\revitalize2019):
    - edit file using VS Code
    - save changes (CTRL-S)
    - click on source code (left side) -- should have number
    - enter Message in box, then click the checkMark above (does 'commit')
    - Open a terminal window, ls to the same directory (above).  
    - enter "git push" and press ENTER (updates will be pushed to GitHub)
    - Open the repository in a browser window, then 
        open the updated file -- verify that the latest 
        changes are present...

attempt to 1st revised push from macbook pro

FROM THE MACBOOK PRO VS Code
    *** (be in ~/WebDevelopment/revitalize2019):
- edit file using VS Code
- save changes (CMD-S)
- click on source code (left side) -- should have number
- enter Message in box, then click the checkMark above (does 'commit')
- Open a terminal window, ls to the same directory (above).  
    - git status
    - git add .
    - git commit -am "<insert comment>"
    - git push
    - enter "git push" and press ENTER (updates will be pushed to GitHub)
    - Open the repository in a browser window, then 
        open the updated file -- verify that the latest 
        changes are present...
